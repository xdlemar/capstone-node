name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 23.94.230.146
          username: loghe5242
          key: ${{ secrets.SSH_KEY }}   # your private key (PEM) stored in repo secrets
          port: 2298
          script_stop: true
          script: |
            set -e
            cd /home/log1.health-ease-hospital.com

            if [ -d .git ]; then
              git pull origin main
            else
              git clone https://github.com/xdlemar/capstone-node.git .
            fi

            # Node & pm2
            npm i -g pm2 || true
            npm ci

            # Migrations (password encoded)
            DATABASE_URL='postgresql://postgres:Masterpogi09%3F@23.94.230.146:5432/inventory_db?schema=public' \
              npx prisma migrate deploy --schema=inventory-svc/prisma/schema.prisma

            DATABASE_URL='postgresql://postgres:Masterpogi09%3F@23.94.230.146:5432/procurement_db?schema=public' \
              npx prisma migrate deploy --schema=procurement-svc/prisma/schema.prisma

            DATABASE_URL='postgresql://postgres:Masterpogi09%3F@23.94.230.146:5432/auth_db?schema=public' \
              npx prisma migrate deploy --schema=auth-svc/prisma/schema.prisma

            # Start / reload with PM2
            pm2 startOrReload ecosystem.config.js --update-env || pm2 start ecosystem.config.js
            pm2 save
